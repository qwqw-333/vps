---
# Placeholder — seal with kubeseal before deploying (see docs/kubernetes.md)
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: couchdb-credentials
  namespace: obsidian-sync
  annotations:
    sealedsecrets.bitnami.com/namespace-wide: "true"
spec:
  encryptedData:
    COUCHDB_USER: PLACEHOLDER_SEAL_WITH_KUBESEAL
    COUCHDB_PASSWORD: PLACEHOLDER_SEAL_WITH_KUBESEAL
  template:
    metadata:
      name: couchdb-credentials
      namespace: obsidian-sync
    type: Opaque
